# Single-stage build: copy a custom HTML file into the nginx web root
FROM nginx:alpine

# Copy our custom page into the default nginx serve directory
COPY index.html /usr/share/nginx/html/index.html
